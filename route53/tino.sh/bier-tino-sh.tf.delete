resource "aws_route53_record" "bier" {
  zone_id = data.aws_route53_zone.tino.zone_id
  name    = "bier.${data.aws_route53_zone.tino.name}"
  type    = "A"
  ttl     = "${var.ttl}"
  records = ["${jsondecode(data.http.ispip.body).addr.ip}"]
}
